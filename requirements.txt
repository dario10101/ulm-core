# Dependencias de ejecucion. Lo de testing y linting va en requirements-dev.txt,
# para que la imagen de produccion no las arrastre (ver deuda D17, Docker).
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.20.0
# zoneinfo necesita la base IANA del sistema, que Windows y las imagenes
# Docker slim no traen. Sin esto la app no arranca. Ver how-to/timezones.md.
tzdata==2026.1
